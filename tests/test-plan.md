# V5.6–V6.0 Test Plan

Five passes:
1. Syntax + ZIP integrity
2. D1 schema + route consistency
3. Authentication + rate limit + approval boundary
4. External credential checks (PASS only when real credentials are present; otherwise SKIP)
5. Regression + release health

A live PASS for Instagram/Telegram/OpenAI cannot be manufactured without real credentials and permissions.
